{
  "name": "google-calendar-combined",
  "version": "1.0.0",
  "description": "Combined view of two Gmail accounts' Google Calendars",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"node server.js\" \"cd client && npm run dev\"",
    "install-all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "googleapis": "^131.0.0",
    "express-session": "^1.18.0"
  }
}
